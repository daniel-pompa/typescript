//* Data Types
// Tuples - an array with a fixed number of elements whose types are known, but need not be the same.

(() => {
  const person: [string, number, boolean] = ['Gael', 2, true];
  console.log(person);
})();
