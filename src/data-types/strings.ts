//* Data Types
// Strings - any sequence of characters within single or double quotes or template literals.

(() => {
  const typeScript: string = 'TypeScript';
  const javaScript: string = 'JavaSCript';
  const message = `${typeScript} es un lenguaje de programación fuertemente tipado que se basa en ${javaScript}`;

  console.log(typeScript.toUpperCase());
  console.log(message);
})();
